#include <stdio.h>

int diff(int n)
{
    
}

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int n;
        scanf("%d", &n);

        printf("%d", diff(n));
    }

    return 0;
}