#include <stdio.h>

int sum(int n, int m)
{
    int nele = n / 2;
    int num = m / nele;
    if ()
}

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int n, m;
        scanf("%d %d", &n, &m);

        printf("%d", sum(n, m));
    }
}